[package]
name = "headless-sim"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
issun = { path = "../../crates/issun", default-features = false, features = ["storage"] }
tokio = { version = "1.35", features = ["full"] }
async-trait = "0.1"
